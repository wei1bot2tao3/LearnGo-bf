
for if 注意点
	if s.isFull() {
		// 我就在这等着
		//有人入队我会被唤醒
		fmt.Println("满了")
		s.notFull.Wait()
	}


超时控制：
超出到规定时间就退出阻塞
怎么控制

// 基于切片

seliceQueue 缺点：
    容量不好预估，过大浪费空间，过小容易阻塞

    自动扩容
    缩荣：小容量了就停止缩荣，比如不能小于 初始容量
    扩容：初期因子要大，（比如初期2倍）后期1.25扩容

// 基于双向链表的并发队列
linked_queue
